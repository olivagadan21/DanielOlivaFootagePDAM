import 'package:flutter/material.dart';

class Buscado extends StatelessWidget {

  const Buscado({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
  
}